<div class="fondo-container">
  <div class="small-container center-block card padding">
    <%= render "partials/imgLogin" %>
    <h2>Iniciar Sesión</h2>

    <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="form-inputs margin-top">
        <%= f.input :email,
                    required: false,
                    autofocus: true,
                    input_html: { autocomplete: "email" },
                    :label =>"Correo Electrónico" %>
        <%= f.input :password,
                    required: false,
                    input_html: { autocomplete: "current-password" },
                    :label => "Contraseña" %>
        <%= f.input :remember_me, :label => "Recordar Sesión",as: :boolean if devise_mapping.rememberable? %>
      </div>

      <div class="form-actions margin-top">
        <%= f.button :submit , "Iniciar" , :class => "color-text"%>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>
